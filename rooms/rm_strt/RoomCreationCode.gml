// Creation code for rm_puzzle

var lyr_instances = layer_get_id("Instances");

// Insert start button
instance_create_layer(159, 383, lyr_instances, obj_strt_btn);

// Insert quit button
instance_create_layer(447, 383, lyr_instances, obj_quit_btn);

// Insert start screen background
instance_create_layer(0, 0, lyr_instances, obj_strt_bg);

